This example illustrates the use Bootstrap in Angular2

To run:

- in a cmd window navigate to the root directory Lesson04
- run "npm start". This will open browser
- in the browser use http://localhost:3000/Lesson04

The css classes of various elements will change when you click on them.
This is achieved by setting the css class in code as an input parameter using the [] notation.

